<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="OverviewDAO">

    <resultMap id="adminOverviewResult" type="adminOverview">

    </resultMap>

  
  

    <select id="selectOverviewList" parameterType="adminOverview" resultType="adminOverview">
        SELECT
		 user_count_tb_date;
	 	 user_count_tb_visitors;
	 	 user_count_tb_joiners;            
        FROM user_count
        WHERE user_count_tb_date BETWEEN ${StartDate} and ${EndDate}
        	
    </select>

  

</mapper>
